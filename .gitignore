.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.idea/
*.iml
out/
.DS_Store
# Spark's own scratch warehouse dir, created by CREATE TABLE statements
# the sqllogictest sweep attempts against spark_catalog (not this connector).
spark-warehouse/
# Claude Code's own local session scratch (per-machine, not project content).
.claude/
